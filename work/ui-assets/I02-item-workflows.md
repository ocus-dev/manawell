# I02 — ComfyUI item icon workflows and candidate generation

Dependency: I01. Read its manifest and the existing local Krea/Comfy helpers and templates. Do not alter the side-view workflow builder or its installed graphs.

## Implement

Create tools/ui_assets/ with a small separate builder/runner and art/ui-assets/workflows/ containing native/API workflow pairs for all seven resource/upgrade/loadout icons. Reuse the installed Krea baseline and existing resumable job transport where practical. Add a shared standalone cutout workflow using installed local nodes; verify mask polarity before writing RGBA. No 3D stage, paid image node or new model install.

Each preset has one isolated complete object, broad value groups, fixed upper-left light, neutral background and ample margin. Match the first-batch subject descriptions; no scene, floating particles, text, card frame, rarity border or baked interaction state. Fixed seeds support comparison, not a guarantee of identity. Save per-job graphs, IDs, outputs and errors; do not silently resubmit uncertain jobs.

Generate one candidate for each of the seven icons sequentially. Inspect at approximate display size; allow at most one additional candidate per failed silhouette/composition. Preserve all outputs and choose provisional sources with brief reasons. Create an actual-size comparison sheet with a larger reference view; mark serious unresolved failures explicitly.

Install only the newly authored native workflows under ComfyUI/user/default/workflows/Telos_UI_Icons if permitted. Finish reviewable files before requesting any needed external-directory permission. Provide import instructions if blocked. Update art/ui-assets/README.md with exact generation/resume commands, editable nodes and where outputs land.

## Acceptance

Native/API links, widget parameters and installed node/model compatibility validate. Seven provisional candidates are generated and visually inspected, or exact blockers are recorded. Outputs/provenance exist; no synthetic claims of readability based only on successful execution. No actor or environment regeneration.

## Stop

No live Godot widgets, skill-image generation or atlas building.

## Completion note

Pending.
