# LD02 handoff

Status: DONE

## Changed files

- Added the campaign manifest, Act 1 file, and nine authored level files under `prototype/data/campaign/`.
- Routed `prototype/scripts/model/campaign_catalog.gd` through `LevelDataLoader` while preserving `get_act`, `get_node`, `node_ids`, act ordering, node IDs, positions, prerequisites, types, and well IDs.
- Routed `ContentCatalog` well definitions through the same resolved campaign source and retained legacy well accessors (`base_output`, `spawn_interval_factor`, `enemy_damage_factor`).
- Removed exact nine / three wells / five monsters / one boss and eight-boss-prerequisite checks. Catalog validation now checks nonempty graph structure, IDs, references, roots, cycles, reachability, and loader-owned level/content validation.
- Replaced suffix-derived campaign loot item level lookup with the authored `rewards.loot.item_level` value.
- Added `prototype/tests/level_data_migration_test.gd` for migration parity, save/unlock compatibility, same-source well identity, and a valid two-node fixture.
- Marked `campaign_definitions.gd` and `campaign_encounters.gd` as legacy migration/runtime compatibility data. They remain available for current combat routing and old tests; CampaignCatalog no longer reads campaign definitions.

## Parity decisions

- Preserved all nine IDs, display names, map positions, prerequisite IDs, node types, well IDs, wave compositions and exact spawn ordering.
- Preserved current mana rewards: 3, 4, 5, 6, 7, and 12 for the five monster nodes and boss; wells remain 0.
- Preserved current item mapping from `ItemCatalog`: Heavy Breech, High-volume Cylinder, Bulwark Plating, Cycle Driver, Fast-cycle Rotor, Runner Frame, Long Accelerator, Anchor Bracing, and Jump Servos. Each authored first-clear guarantee uses the same base ID and the current common, quantity-one behavior.
- Preserved current item levels from the old node-level behavior: nodes 1-3 level 1, nodes 4-6 level 2, nodes 7-9 level 3. Runtime now reads the explicit field instead of deriving it from a suffix.
- Preserved boss profile values in authored data: 240 health, 18 attack damage, 3-second attack interval. Boss spawn identity is `crown_guardian`.
- Preserved well values: `well_1` output 2 / spawn factor 1 / damage factor 1; `well_2` output 4 / spawn factor 0.8 / damage factor 1.25; `well_3` output 5 / spawn factor 0.7 / damage factor 1.4. Machine integrity and sealing remain 150 and 2 seconds.
- No kill quotas, new gameplay stats, LD03 scaling, LD04 objectives, LD05 delivery, LD06 snapshots, LD07 UI, or LD08 acceptance behavior was enabled.

## Checks and results

- `res://tests/level_data_loader_test.gd`: PASS, exit 0.
- `res://tools/validate_level_data.gd -- res://data/campaign`: PASS, exit 0; one act loaded.
- `res://tests/level_data_migration_test.gd`: PASS, exit 0. It compares all migrated node identity/prerequisite/map values, waves, mana, first-clear base IDs, boss values, well values, save progress/unlock behavior, and loads a valid two-node fixture.
- `res://tests/campaign_state_test.gd`: PASS, exit 0.
- `res://tests/content_catalog_test.gd`: PASS, exit 0.

## Remaining issues

- Existing combat execution still uses the clearly labeled legacy encounter table for runtime waves and boss setup; LD03 owns routing finite waves and boss profiles from resolved level data.
- Existing first-clear guaranteed item delivery is intentionally not wired by LD02; LD05 owns entitlements, generation, pending inventory delivery, and claim provenance.
- Snapshot freezing and migration remain deferred to LD06. The legacy campaign JSON source is now authoritative for catalog and Operations well identity, but active-run snapshot behavior is unchanged.
