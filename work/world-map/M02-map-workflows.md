# M02 — Build map-generation workflows and design candidates

Depends on M01. Read existing `tools/asset_pipeline` Comfy transport and native/API workflow conventions, then implement in `tools/world_map/` without editing shared generation tools.

Create editable native/API ComfyUI pairs in `art/world-map/workflows/`: an act-map concept workflow plus a reference-guided revision workflow using installed capabilities. Preserve world/style/theme/composition prompt blocks and expose seed, dimensions and reference. Revision must actually condition on the selected reference, not merely mention it in text; if the installed stack cannot do this, record that limitation and provide deterministic prompt/seed variants without claiming reference conditioning. Store graph, seed, model identifiers, job ID and image hashes per take. Preserve masters and regenerate into new folders.

Generate three Act 1 alternatives with the same brief and nine-node overlay contract: geography should explore perceived scale and route composition, not just color. Use the installed model's appropriate size, then deterministically prepare the agreed master dimensions without stretching. Record any resampling. Overlay route and placeholder markers in a separate preview image; keep the clean background. If a candidate requires different marker coordinates, save its own layout revision and validate it.

Install new native graphs in ComfyUI's `Telos_WorldMap` folder when permitted, preserving existing folders. Make commands reproducible and resumable; do not silently resubmit uncertain jobs. Limit this initial batch to three concepts and at most one corrective render for an unusable output. Further iterations belong to owner feedback after M03.

Acceptance: native links, API inputs and installed models validated; completed generation evidence for three candidates or exact blocker; clean masters and overlay previews available; documentation explains creating a new act theme or revising an approved candidate. Do not select a final design on behalf of the owner. Stop before runtime imports.

Completion note: DONE. Built two native/API pairs in `art/world-map/workflows/` and installed native files in ComfyUI `Telos_WorldMap`: concept and actual latent image-conditioned revision. Generated three serial Krea2 candidates (valley, coast, terraces), preserving sources, graphs, seeds, models, receipts/histories and hashes. Prepared uniform 2048×1152 masters plus separate full-size/1280 overlays and landmark-adjusted manifests. `tools/world_map/verify_delivery.py` passed workflow and candidate checks. Source/overlay art visually inspected; no corrective render needed. Commands and tradeoffs are in `art/world-map/README.md`, actual validation in `VALIDATION.md`. Revision graph structurally checked but no revision generation or manual Comfy canvas test performed. No live game edits or design approval. Stop for owner iteration before integration.
