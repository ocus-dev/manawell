# W03 handoff

Status: DONE

## Changed files

- `prototype/scenes/tools/weapon_designer.tscn`: standalone designer scene.
- `prototype/scripts/tools/weapon_designer.gd`: authoring/job view with source path and crop metadata, text and behavior fields, base stats, rarity/level/affixes, grip/facing/world scale, icon/equipped-stat previews, draft actions, job list/progress/failure/resume and asynchronous worker launch.
- `prototype/scripts/tools/weapon_designer_store.gd`: safe repository-local JSON draft and job receipt persistence plus W01 validation payload construction; no profile or runtime catalog mutation.
- `prototype/tests/weapon_designer_test.gd`: focused W03 acceptance coverage.
- `work/weapon-flow/README.md`: W03 marked DONE.

## Checks actually run

- `Godot_v4.8-dev4_win64_console.exe --headless --path prototype --script res://tests/weapon_designer_test.gd`: PASS; no script, parse or assertion errors.
- `Godot_v4.8-dev4_win64_console.exe --headless --path prototype --scene res://scenes/tools/weapon_designer.tscn --quit-after 2`: PASS; no script, parse or resource-load errors.

## Boundaries and blockers

- Drafts default to `res://data/designer/drafts/`; running receipts default to `res://data/designer/jobs/`. Tests use isolated `user://` roots. IDs reject path separators, `..`, drive separators and overlong names.
- Metadata edits preserve prepared-art references and do not rerun preparation. Receipts are written before worker launch and retained when the runner is unavailable. The worker launch is asynchronous so the view remains responsive.
- Preview uses `HeroStatResolver` with an isolated synthetic authored instance. The designer does not touch account state, save profiles, publication indexes, runtime assets or production drops.
- `Add to game (W04 unavailable)` is disabled and has no publication implementation.
- W02's manifest/progress protocol has no Godot polling adapter yet; W04 should own the validated package bridge and publication wiring. Manual visual review and real ComfyUI execution remain W07 work.

## Next-card interfaces

- W04 can consume `WeaponDesignerStore.revision_for(draft)`, `recipe_for(draft)`, `validate_authored(draft)`, saved draft JSON, and receipt `job_id`/`status`/`stage`/`progress`/`failure` fields.
- W04 should replace the disabled action with transactional staging and keep the current draft/job persistence paths compatible.